---
navigationLabel: home
---

import { Link as GatsbyLink } from "gatsby";
import { mix } from "@theme-ui/color";
import { PostCard } from "../components/PostCard";
import { ProjectCard } from "../components/ProjectCard";
import { format } from "date-fns";

<Alert variant="success">I'm currently looking for new opportunities 😊</Alert>
<Divider />

# [akshay-ashok](#akshay-ashok)

Full Stack Developer based in India

Microsoft Learn Student Ambassador, React, NodeJS, Express, MongoDB, PostgreSQL, Python, Django, Apollo/GraphQL.

<Button
  as="a"
  variant="success"
  href="/resume"
  sx={{
    mr: 2,
  }}
>
  View CV/Resume
</Button>

<Link href="/akshay-cv.pdf" target="_blank" title="View CV">
  .pdf version
</Link>
<Divider />
<Divider />

## [intro](#intro)

hey there! 👋 I'm Akshay, a self-taught full stack developer based in India. I like to build and develop
cool products. I mainly work with ReactJS for frontend and NodeJS or Python depending on the task.

<Divider />

<Flex
  sx={{ flexWrap: 'wrap'}}
>
<Box
  sx={{
    width: ["100%", "100%", "55%"],
    mb: 3,
    pr: [0,0, 4],
  }}
>

## contact

<Text>Email</Text>
<Link href="mailto:howdy.aks@gmail.com" target="_blank" variant="styles.a">
  howdy.aks@gmail.com
</Link>
<Divider />

<Text>Twitter</Text>
<Link href="https://twitter.com/aks2899" target="_blank" variant="styles.a">
  https://twitter.com/aks2899
</Link>
<Divider />

<Text>GitHub</Text>
<Link href="https://github.com/Akshay090" target="_blank" variant="styles.a">
  https://github.com/Akshay090
</Link>
<Divider />

<Divider />
</Box>

<Box
  sx={{
    width: ["100%", "100%", "45%"],
    mb: 3
  }}
>

## blog topics

<Divider />

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag;
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("secondary", "primary", `${index / tags.length}`),
                borderColor: mix(
                  "secondary",
                  "primary",
                  `${index / tags.length}`
                ),
              }}
            >
              {name}
            </Badge>
          );
        })}
    </Box>
  )}
</SourceTags>

<Divider />
<Divider />

</Box>
</Flex>

## projects

<SourceList filter="projects">
  {(projects) => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: (theme) => `-${theme.space[2]}px`,
        mr: (theme) => `-${theme.space[2]}px`,
      }}
    >
      {projects.splice(0, 6).map((edge, index) => {
        const {
          frontmatter: { title, misc, date, featuredImageUrl },
          fields: { slug },
        } = edge.node;
        return (
          <ProjectCard
            key={index}
            index={index}
            cardImage={featuredImageUrl}
            title={title}
            misc={misc}
            date={date}
            slug={slug}
          />
        );
      })}
    </Flex>
  )}
</SourceList>

<Divider />
<Flex
  sx={{
    justifyContent: "center",
  }}
>
  <Box>
    <Button as={GatsbyLink} variant="ghost" href="/projects">
      All Projects{" "}
      <Box as="span" role="img" aria-label="pencil">
        🛠
      </Box>
    </Button>
  </Box>
</Flex>
<Divider />

## latest posts

<Divider />

<SourceList filter="posts">
  {(posts) => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: (theme) => `-${theme.space[2]}px`,
        mr: (theme) => `-${theme.space[2]}px`,
      }}
    >
      {posts
        .filter((edge) => !edge.node.frontmatter.isPrivate)
        .reduce((posts, post) => {
          return post.node.frontmatter.pinned
            ? [post, ...posts]
            : [...posts, post];
        }, [])
        .splice(0, 4)
        .map((edge, index) => {
          const {
            frontmatter: {
              title,
              featuredImageUrl,
              tags,
              date,
              dateModified,
              pinned,
            },
            excerpt,
            timeToRead,
            fields: { slug },
          } = edge.node;
          return (
            <PostCard
              key={index}
              title={title}
              cardImage={index === 0 ? featuredImageUrl : null}
              tags={tags}
              date={date}
              dateModified={dateModified}
              excerpt={excerpt}
              slug={slug}
              pinned={pinned}
              timeToRead={timeToRead}
            />
          );
        })}
    </Flex>
  )}
</SourceList>
<Divider />
<Flex
  sx={{
    justifyContent: "center",
  }}
>
  <Box>
    <Button as={GatsbyLink} variant="ghost" href="/posts">
      More posts{" "}
      <Box as="span" role="img" aria-label="pencil">
        ✏️
      </Box>
    </Button>
  </Box>
</Flex>
